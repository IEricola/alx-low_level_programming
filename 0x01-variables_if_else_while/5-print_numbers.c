#include <stdio.h>
/**
 * main - Entr point of action
 *
 * Return: Is always 0, if successful
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}
