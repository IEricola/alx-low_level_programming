#include "main.h"

/**
 * _isdigit - Check if aum is a digit
 * @x: The umber to be checked
 * Return: 1 for a character that will be adigit or 0 for ay else
 */

int _isdigit(int x)
{
	if (x >= 48 && x <= 57)
	{
	return (1);
	}
	return (0);
}
